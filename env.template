# HuggingFace Configuration
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_huggingface_token_here
HF_USERNAME=your_hf_username

# Target Platform Configuration
# Replace these with your target platform credentials

# For GitLab-based platforms (OpenXlab, custom GitLab, etc.)
# Get token from: Settings -> Access Tokens
TARGET_USERNAME=your_target_username
TARGET_TOKEN=glpat-your_gitlab_token_here

# For ModelScope (if using ModelScope as target)
# TARGET_USERNAME=your_modelscope_username
# TARGET_TOKEN=your_modelscope_token

# For other platforms
# Adjust the USERNAME and TOKEN according to your platform's authentication method
# Some platforms may only need TOKEN without USERNAME

# GitLab Remote Mirroring (optional)
# Enable when using --use-remote-mirror
MIRROR_PLATFORM=gitlab
GITLAB_API_BASE=https://nm.aihuanxin.cn/api/v4
# Format: <group>/<subgroup>/.../<project>
GITLAB_PROJECT_PATH=qdlake/repo/llm_model/maoxin/Intern-S1
# Defaults to TARGET_TOKEN if not set
# GITLAB_API_TOKEN=glpat-your_gitlab_api_token
# Optional regex to limit mirrored branches
# GITLAB_MIRROR_BRANCH_REGEX=.* 

# LFS behavior (optional)
# Set to 1 to skip downloading/pushing Git LFS blobs (pointer-only mirrors).
# Default is 0, which downloads all LFS objects before pushing.
GIT_LFS_SKIP_SMUDGE=0

# Notes:
# 1. If your target URL already includes credentials (e.g., https://user:token@platform.com/repo.git),
#    you don't need to set TARGET_USERNAME and TARGET_TOKEN
# 2. Keep this file secure and never commit it to version control
# 3. You can copy this file to .env and fill in your actual credentials

